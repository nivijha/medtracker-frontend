import Login from '../components/ui/Login';

export const metadata = {
  title: 'Login - MedTracker',
  description: 'Sign in to your MedTracker account',
};

export default function LoginPage() {
  return <Login />;
}